#include <stdio.h>
int main() {
    int a,b,c,max;
    max=a;
    scanf("%d%d%d",&a,&b,&c);
    if(b<max){
        printf("%d",max);
    }else if(c<max){
        printf("%d",max);
    }

    return 0;
}