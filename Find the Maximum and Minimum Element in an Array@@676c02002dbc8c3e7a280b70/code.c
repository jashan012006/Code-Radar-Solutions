#include<stdio.h>
int main(){
    int n,k;
    scanf("%d",&n);
    int arr[n];
    int min=arr[0],max=arr[0];
    for(int i=0;i<n;i++){
        scanf("%d",&arr[i]);
        if(arr[i]<min){
            
        }else if(arr[i]>max){

        }
        }printf("%d",min);
        printf("%d",max);
        }

          
